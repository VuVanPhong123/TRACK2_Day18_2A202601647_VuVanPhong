# Submission screenshots

Rubric requires at least one screenshot under this directory.

For the default lightweight path, capture terminal evidence after all notebooks pass:

1. The `_lakehouse/` directory tree showing Bronze/Silver/Gold and Delta table layout.
2. The contents of at least one `_lakehouse/**/_delta_log/*.json` commit file.

Do not commit the generated `_lakehouse/` directory itself; only commit the screenshot evidence and the eight executed `notebooks/*.ipynb` files with outputs preserved.